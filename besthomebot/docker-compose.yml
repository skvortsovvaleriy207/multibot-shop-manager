services:
  besthomebot:
    deploy:
      resources:
        limits:
          cpus: '0.33'
          memory: '256M'
    build: .
    container_name: besthomebot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - BOT_NAME=besthomebot
    volumes:
      - .:/app
      - ../shared_storage:/shared_storage
